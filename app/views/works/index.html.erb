<h2 class="text-2xl font-bold mb-6"><%= t("titles.work_list") %></h2>

<% content_for :body_data do %>
  data-controller="works-index"
<% end %>

<div class="actions mb-4">
  <%= link_to t("buttons.new"), new_work_path, class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>
</div>

<% if @works.any? %>
  <% @works.each do |work| %>
    <div class="flex justify-between items-center border-b py-2">
      <!-- 左側：タイトル -->
      <span><%= work.title %></span>

      <!-- 右側：ボタン -->
      <div class="flex space-x-2">          
        <%= link_to t("buttons.edit"), edit_work_path(work), class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 mr-2" %>
        <%= button_to t("buttons.destroy"),
            work_path(work),
            method: :delete,
            data: { confirm: "本当に削除しますか？" },
            class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
      </div>
    </div>
  <% end %>
<% else %>
  <p>まだアイデアがありません</p>
<% end %>

<div class="mt-4">
  <%= link_to t("buttons.to_post"), posts_path,
              class: "inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
</div>
