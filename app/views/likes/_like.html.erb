<div id="like-button-<%= post.id %>">
  <% if post.liked_by?(current_user) %>
    <%= button_to post_like_path(post, post.likes.find_by(user: current_user)),
                  method: :delete,
                  data: { turbo_stream: true },
                  class: "flex items-center gap-2 text-red-500 font-semibold transition" do %>
      ❤️ <span> (<%= post.likes.count %>)</span>
    <% end %>
  <% else %>
    <%= button_to post_likes_path(post),
                  method: :post,
                  data: { turbo_stream: true },
                  class: "flex items-center gap-2 text-gray-600 hover:text-red-500 font-semibold transition" do %>
      🤍 <span> (<%= post.likes.count %>)</span>
    <% end %>
  <% end %>
</div>
